name: Tests

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run shell tests
        run: tests/agentsy-test.sh

      - name: Run ShellCheck
        run: shellcheck agentsy scripts/openai-copilot scripts/lib/*.sh tests/*.sh
